{
    "name": "PTECop all errors",
    "rules": [
        {
            "id": "PTE0012",
            "action": "None",
            "justification": "Ignore for TestApp: The InternalsVisibleTo setting will expose your internal objects to any extension with the given name, publisher, and ID. Access modifiers are not designed to be used as a security boundary, but for API development."
        }
    ]
}